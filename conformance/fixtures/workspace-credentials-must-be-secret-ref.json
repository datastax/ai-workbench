{
	"slug": "workspace-credentials-must-be-secret-ref",
	"description": "`credentials` values must match `<provider>:<path>` (e.g. `env:ASTRA_TOKEN`). A raw token value is rejected with 400 validation_error — it never reaches the SecretResolver.",
	"captures": [
		{
			"request": {
				"body": {
					"credentials": {
						"token": "raw-token-here"
					},
					"kind": "astra",
					"name": "w"
				},
				"method": "POST",
				"path": "/api/v1/workspaces"
			},
			"response": {
				"body": {
					"error": {
						"code": "validation_error",
						"message": "credentials.token: expected '<provider>:<path>', e.g. 'env:FOO'",
						"requestId": "{{REQID_1}}"
					}
				},
				"status": 400
			},
			"step": 1
		},
		{
			"request": {
				"body": null,
				"method": "GET",
				"path": "/api/v1/workspaces"
			},
			"response": {
				"body": {
					"items": [],
					"nextCursor": null
				},
				"status": 200
			},
			"step": 2
		}
	]
}
